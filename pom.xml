<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>com.healthcare</groupId>
    <artifactId>activity-tracker</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>activity-tracker</name>
    <description>Healthcare Activity Tracking System</description>

    <properties>
        <java.version>17</java.version>
        <jjwt.version>0.13.0</jjwt.version>
        <!-- Explicitly pin Lombok version so the annotationProcessorPaths entry
             resolves correctly in all IDEs (Eclipse, VS Code Java) and CI builds.
             The Spring Boot BOM manages a version too; override here if you need a newer patch. -->
        <lombok.version>1.18.48</lombok.version>
        <spotless.version>2.43.0</spotless.version>
        <!-- Overrides the version the Spring Boot parent manages, keeping it explicit
             alongside the other pinned build-plugin versions. -->
        <jacoco.version>0.8.12</jacoco.version>
        <google-java-format.version>1.22.0</google-java-format.version>
        <!-- springdoc 2.8.x tracks Spring Boot 3.5; the 3.x line targets Boot 4. -->
        <springdoc.version>2.8.17</springdoc.version>

        <!-- Patch-level overrides of Spring Boot BOM-managed versions. Each is the newest
             release in the same minor line the BOM already pins, bumped to pick up security
             fixes that landed after Boot 3.5.16 was cut. Revisit on the next Boot upgrade:
             once the BOM catches up these can be deleted. -->
        <tomcat.version>10.1.59</tomcat.version>
        <netty.version>4.1.137.Final</netty.version>
        <postgresql.version>42.7.13</postgresql.version>
        <log4j2.version>2.26.1</log4j2.version>
        <!-- CVE-2025-48924: uncontrolled recursion in ClassUtils.getClass. Boot 3.5.16
             pins 3.17.0; commons-lang3 fixed it in 3.18.0. -->
        <commons-lang3.version>3.18.0</commons-lang3.version>
        <!-- CVE-2026-54515 in jackson-databind. Boot 3.5.16 pins the Jackson BOM at
             2.21.4; the fix ships in 2.21.5. Overriding the BOM property moves every
             Jackson module together, keeping databind/core/annotations in lockstep. -->
        <jackson-bom.version>2.21.5</jackson-bom.version>

        <!-- swagger-ui is NOT managed by the Spring Boot BOM, so this property alone
             does nothing - springdoc pins 5.32.2 in its own dependencyManagement and that
             is what we would otherwise inherit. The dependencyManagement entry below is
             what actually moves it; the property just keeps the number in one place.
             5.32.14 bundles DOMPurify 3.4.13, clearing the DOMPurify CVEs that the webjar
             drags in. Drop both once springdoc ships a swagger-ui at least this new. -->
        <swagger-ui.version>5.32.14</swagger-ui.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Forces the swagger-ui webjar that springdoc-openapi-starter-webmvc-ui
                 pulls in transitively up to a build with a patched DOMPurify. Our own
                 dependencyManagement wins over a transitive dependency's, so this is
                 enough without declaring swagger-ui as a direct dependency. -->
            <dependency>
                <groupId>org.webjars</groupId>
                <artifactId>swagger-ui</artifactId>
                <version>${swagger-ui.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- JPA + Postgres -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Security -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Database Migrations -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <!-- Flyway 10 split per-database support out of flyway-core. Without this module
             Flyway aborts with "Unsupported Database: PostgreSQL 16.x" at startup. Version
             is managed by the Spring Boot BOM. -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!-- Actuator -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Redis (distributed token blacklist; rate limiting is per-instance in-memory,
             see RateLimitingFilter javadoc) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>

        <!-- Kafka (event publishing for activity creation) -->
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka</artifactId>
        </dependency>

        <!-- Rate Limiting -->
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j-core</artifactId>
            <version>8.10.1</version>
        </dependency>

        <!-- OpenAPI / Swagger UI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>

        <!-- CSV import (bulk-loading external exports, e.g. Kaggle Fitbit datasets) -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-csv</artifactId>
            <version>1.11.0</version>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Testcontainers: repository/migration tests against real PostgreSQL.
             These tests are skipped automatically when Docker is unavailable. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Allow Byte Buddy (used by Mockito) to instrument classes on Java 17+ -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- @{argLine} is late-binding: surefire expands it at runtime to the
                         argLine property, which jacoco:prepare-agent sets to the coverage
                         agent. A literal value here would silently discard the agent and
                         report 0% coverage with no error. Keep the placeholder first. -->
                    <argLine>
                        @{argLine}
                        -Dnet.bytebuddy.experimental=true
                        --add-opens java.base/java.lang=ALL-UNNAMED
                        --add-opens java.base/java.lang.reflect=ALL-UNNAMED
                        --add-opens java.base/java.util=ALL-UNNAMED
                    </argLine>
                </configuration>
            </plugin>
            <!-- Coverage for SonarQube. prepare-agent must run before surefire, so it binds
                 to initialize; report binds to test rather than the default verify, because
                 verify also runs Spotless and Checkstyle, which fail the build on violations
                 and would abort a scan before the report exists. -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>

            <!-- Code Formatting: enforce Google Java Format on every verify -->
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${spotless.version}</version>
                <configuration>
                    <java>
                        <googleJavaFormat>
                            <version>${google-java-format.version}</version>
                            <style>GOOGLE</style>
                        </googleJavaFormat>
                    </java>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <phase>verify</phase>
                    </execution>
                </executions>
            </plugin>

            <!-- Style Checks: Google Checks via Checkstyle -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>3.3.1</version>
                <configuration>
                    <configLocation>google_checks.xml</configLocation>
                    <consoleOutput>true</consoleOutput>
                    <failsOnError>true</failsOnError>
                    <includeTestSourceDirectory>true</includeTestSourceDirectory>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <phase>verify</phase>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.owasp</groupId>
                <artifactId>dependency-check-maven</artifactId>
                <version>12.1.3</version>
                <configuration>
                    <failBuildOnCVSS>7</failBuildOnCVSS>
                    <suppressionFile>dependency-check-suppressions.xml</suppressionFile>
                    <!-- Sonatype OSS Index rejects anonymous requests with 401, which
                         dependency-check raises as a fatal AnalysisException. It has never
                         returned data in this pipeline - earlier runs logged the same failure
                         as warnings, masked by the CVSS gate failing first - so disabling it
                         costs no coverage we actually had. NVD remains the data source.
                         Re-enable with ossIndexServerId credentials if that account is set up. -->
                    <ossindexAnalyzerEnabled>false</ossindexAnalyzerEnabled>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
